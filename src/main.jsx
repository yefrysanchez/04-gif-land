import React from "react";
import ReactDOM from "react-dom/client";
import GifLandApp from "./components/GifLandApp";
import "./styles.css"

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <GifLandApp />
  </React.StrictMode>
);
